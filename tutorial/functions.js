/* const printName = function printMyName() {
    console.log("sujal thakkar");
};

// printMyName();
const myName = printName;

function addTwoNumbers(a, b) {
    return a + b;
}

console.log(addTwoNumbers(2, 8)) */

/* const validate = function loggedInOrNot(username) {
    if(username === undefined || username.trim().length === 0) return `Please enter a valid username`;
    else return `${username} just logged in`
}

console.log(validate('sujal')); */

addOne(5);
function addOne(val) {
    console.log(val + 1);
    return;
}

